process {
	withLabel: modelling {
		time = '1h'
	}
	withLabel: retry_increasing_cpu {
       		errorStrategy = 'retry'
		cpus   = {2 + task.attempt}
        	maxRetries = { task.exitStatus == 140 ? 5 : 2 }
	}
}

